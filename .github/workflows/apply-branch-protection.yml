# Aplica reglas de protección en `main` vía API (sin entrar a Settings).
# Requiere un secreto con permiso de administración del repositorio; el GITHUB_TOKEN del workflow no alcanza.
#
# Secreto: BRANCH_PROTECTION_TOKEN
#   Fine-grained PAT: permiso "Administration" del repositorio → Read and write.
#   Classic PAT: scope repo (dueño del repo).
#
# Disparadores: manual (Actions → Run workflow) o cron semanal (idempotente).

name: Aplicar branch protection

on:
  workflow_dispatch:
  schedule:
    - cron: "45 8 * * 1"

permissions:
  contents: read

concurrency:
  group: apply-branch-protection
  cancel-in-progress: true

# Las acciones checkout/setup-node aún pueden ejecutarse con Node 20 por defecto;
# GitHub recomienda esta variable hasta que el runner use Node 24 por defecto.
# Ver: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      # No usar `secrets` en `jobs.*.if` — GitHub lo rechaza al validar el workflow.
      - name: Aplicar política en main
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          BRANCH_PROTECTION_TOKEN: ${{ secrets.BRANCH_PROTECTION_TOKEN }}
          BRANCH: main
          REQUIRE_PR: "1"
          STRICT_UP_TO_DATE: "1"
          ENFORCE_ADMINS: "1"
        run: |
          if [ -z "$BRANCH_PROTECTION_TOKEN" ]; then
            echo "::notice::Sin secreto BRANCH_PROTECTION_TOKEN: no se aplica nada (salida OK)."
            exit 0
          fi
          node scripts/apply-branch-protection.mjs
