# Binarios nativos de dependencias (nunca versionar)
*.node

# Dependencies
node_modules
.pnp
.pnp.js

# Next.js
.next
out
*.tsbuildinfo

# Env / local
.env
.env*.local

# Snapshot local dev (directorio socios sin Redis)
.redalia-cache

# Snapshot MCP → web (copiá desde kiteprop-mcp-public-snapshot.example.json; no commitear datos reales)
data/kiteprop-mcp-public-snapshot.json

# Debug / OS
npm-debug.log*
.DS_Store
*.pem

# Vercel
.vercel

# IDE
.idea
.vscode
